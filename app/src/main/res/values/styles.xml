<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Кнопка Material 3 (filled), на всю ширину -->
    <style name="ButtonFilledMaterial" parent="Widget.Material3.Button">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:textSize">16sp</item>
        <item name="android:textAllCaps">false</item>
        <item name="cornerRadius">12dp</item>
    </style>

    <!-- Поле ввода в диалогах -->
    <style name="EditTextStandard" parent="Widget.AppCompat.EditText">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:padding">12dp</item>
        <item name="android:textSize">16sp</item>
        <item name="android:backgroundTint">?attr/colorPrimary</item>
    </style>

    <!-- Подписи нижней навигации -->
    <style name="BottomNavText" parent="TextAppearance.Material3.LabelSmall">
        <item name="android:textSize">12sp</item>
    </style>

</resources>
