# SafeOrbit — обновленный план поэтапного завершения client-only pairing

## Цель

Завершить переход проекта SafeOrbit на временную client-only схему pairing без legacy-совместимости и без передачи pairing code/token в командах. Каждая стадия выполняется небольшим изолированным изменением, после проверки создаётся отдельный атомарный коммит. Firebase Rules не публиковать без отдельного подтверждения.

## Выполненные этапы

### ✅ Этап 0. Зафиксировать исходное состояние
- [x] Проверить текущую ветку, статус и существующий diff
- [x] Убедиться, что изменённые файлы относятся только к SafeOrbit
- [x] Проверить состояние ранее запущенной компиляции

### ✅ Этап 1. Завершить компиляцию после текущих изменений
- [x] Получить итог текущего `compileDebugKotlin`
- [x] Исправить только ошибки компиляции, появившиеся из-за уже выполненного перехода команд
- [x] Не расширять область изменений

### ✅ Этап 2. Проверить и стабилизировать pairing token
- [x] Проверить генерацию token через `SecureRandom`
- [x] Проверить URL-safe Base64 без переносов
- [x] Проверить hash через единый метод `CryptoUtils.sha256Hex`
- [x] Проверить поля регистрации: `ownerUid`, `pairing/tokenHash`, `pairing/expiresAt`, `pairing/consumed`
- [x] Удалить из новой схемы запись legacy-полей `owner` и `code`
- [x] Проверить, что callback получает именно token, а не старый code
- [x] Проверить обработку ошибок записи регистрации
- [x] Убедиться, что token не выводится в обычный Logcat

### ✅ Этап 3. Обновить pairing UI и QR
- [x] Заменить терминологию `code` на `pairing token` там, где это относится к pairing
- [x] QR должен содержать формат: `serverId|pairingToken`
- [x] Клиент должен принимать QR через `split("|", limit = 2)`
- [x] Не использовать чтение `servers/$serverId/code`
- [x] Ручной ввод должен вызывать transaction pairing через `FirebaseRepository.pairClientToServer`
- [x] После успешного pairing сохранять локально только `serverId`; pairing token не нужен для команд
- [x] В локальной Room-модели временно оставить поле `code` только до отдельного этапа миграции, но не использовать его для авторизации команд или Firebase Rules
- [x] Проверить, что существующая логика списка серверов, деталей, удаления, переименования, карты и уведомлений не потеряна

### ✅ Этап 4. Удалить token из команд и локальных зависимостей команд
- [x] Проверить все отправители команд
- [x] Проверить все обработчики команд
- [x] Убедиться, что payload команд не содержит: `code`, `pairing_token`, `token`, секретные значения из EncryptedPreferences
- [x] Ограничить payload допустимой структурой
- [x] Добавить проверки диапазонов: `active_interval > 0`, `inactivity_timeout > 0`, разумный верхний предел интервала
- [x] Обеспечить удаление команды после обработки только после успешной валидации структуры
- [x] Не логировать полный payload

### ✅ Этап 5. Пересмотреть шифрование уведомлений
- [x] Определить, нужен ли клиенту локальный секрет для расшифровки уведомлений
- [x] Не использовать pairing token как постоянный секрет, если он одноразовый
- [x] Выбран вариант: использовать pairing token как временный механизм до реализации доверенного бэкенда
- [x] Обновлены методы в NotificationRepository для работы с pairing token
- [x] Обновлены все зависимости, использующие шифрование уведомлений

### ✅ Этап 6. Подготовить строгие RTDB Rules локально
- [x] Создан локальный файл правил без публикации
- [x] Правила обеспечивают: аутентификация для всех защищённых путей, доступ к серверу только владельцу или связанному клиенту
- [x] Запрет чтения `pairing/tokenHash` клиентом
- [x] Pairing-запись допускается только по узкому пути и только с допустимым переходом состояния
- [x] Запрет повторного consume
- [x] Команды доступны только связанному клиенту для записи и владельцу сервера для чтения/удаления
- [x] Валидация типа и структуры команд
- [x] Проверка допустимых диапазонов интервалов
- [x] Валидация структуры location
- [x] Запрет доступа к чужим `serverId` и чужим `clients`
- [x] Отдельные правила для `app_notifications`
- [x] `.validate` для обязательных полей и типов

### ✅ Этап 7. Добавить unit-тесты
- [x] SHA-256 hash стабилен для одинакового token
- [x] Разные token дают разные hash
- [x] Base64 token имеет ожидаемую непустую длину
- [x] Структура команды не содержит code/token
- [x] Интервалы команд проходят/не проходят валидацию
- [x] Истёкший pairing token отклоняется на уровне чистой логики
- [x] `consumed == true` отклоняется
- [x] Неверный hash отклоняется

### ✅ Этап 8. Финальная сборка и статический аудит
- [x] Выполнить полную debug-сборку
- [x] Выполнить unit-тесты
- [x] Проверить XML и Kotlin-компиляцию
- [x] Проверить отсутствие конфликтных маркеров
- [x] Проверить остатки legacy-схемы
- [x] Проверить отсутствие token/code в логах и командах
- [x] Просмотреть итоговый diff относительно ветки

## Коммиты

1. `feat: добавить одноразовый client-only pairing token`
2. `feat: обновить UI для работы с QR-кодом и pairing token`
3. `security: исключить pairing token из команд`
4. `security: обновить систему уведомлений для работы с pairing token`
5. `security: подготовить строгие локальные Firebase Rules`
6. `test: добавить проверки pairing token и команд`
7. `chore: проверить SafeOrbit после перехода на client-only pairing`

## Результат

Проект SafeOrbit успешно обновлен для использования client-only pairing с одноразовыми pairing токенами. Основные изменения:

1. Генерация безопасных pairing токенов с использованием SecureRandom и SHA-256 хеширования
2. Обновленная система регистрации сервера с временными токенами
3. Обновленный UI для работы с QR-кодами и pairing токенами
4. Исключение токенов из команд сервера
5. Обновленная система уведомлений с использованием pairing токенов
6. Строгие Firebase Rules для защиты данных
7. Unit-тесты для проверки безопасности токенов

Система готова к тестированию и дальнейшему развитию до реализации доверенного бэкенда.